package search;

import java.util.ArrayList;

import exception.NodeNotFoundException;
import filesystem.Dir;
import filesystem.Node;

public class Search {
	/*public Node findNode (Dir root, String name) throws NodeNotFoundException{
		Node findnode = null;
		for (Node node : root.getInsertedNode())
			if (node.getName().equals(name)) {
				findnode = node;
				break;
			}
			else findnode = findNode((Dir)node, path);
		}
		if (findnode != null) return findnode;
		else throw new NodeNotFoundException();
	}*/
}
